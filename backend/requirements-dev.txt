# Development / test dependencies. Install on top of requirements.txt:
#   .venv\Scripts\python -m pip install -r backend\requirements.txt -r backend\requirements-dev.txt
-r requirements.txt

pytest==9.1.1
httpx==0.28.1
# Imported directly by backend/tests/test_compose_env.py (Phase 15.2) to
# parse docker-compose.yml — already installed as an mlflow dependency, pinned
# here at that same version so the test never relies on a transitive pull.
PyYAML==6.0.3

# Linting (Phase 11 CI) — see pyproject.toml for config
ruff==0.16.3
